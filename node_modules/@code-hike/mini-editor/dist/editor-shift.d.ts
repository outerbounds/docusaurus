import { CodeConfig, CodeStep } from "@code-hike/smooth-code";
import React from "react";
export declare type CodeFile = CodeStep & {
    name: string;
};
declare type EditorPanel = {
    tabs: string[];
    active: string;
    heightRatio: number;
};
export declare type EditorStep = {
    files: CodeFile[];
    northPanel: EditorPanel;
    southPanel?: EditorPanel;
    terminal?: string;
};
declare type Tab = {
    title: string;
    active: boolean;
    style: React.CSSProperties;
};
declare type OutputPanel = {
    tabs: Tab[];
    style: React.CSSProperties;
    children: React.ReactNode;
};
declare type Transition = {
    northPanel: OutputPanel;
    southPanel?: OutputPanel | null;
};
export declare function useTransition(ref: React.RefObject<HTMLDivElement>, prev: EditorStep, next: EditorStep, t: number, backward: boolean, codeConfig: CodeConfig): Transition;
export {};
