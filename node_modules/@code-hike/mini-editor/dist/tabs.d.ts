import { Snapshot, Tab } from "./editor-frame";
export { getTabs };
declare function getTabs(prevSnapshot: Snapshot, nextSnapshot: Snapshot, northActive: string, southActive: string | undefined, t: number): {
    northTabs: Tab[];
    southTabs: Tab[];
};
