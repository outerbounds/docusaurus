import React from "react";
declare function MiniTerminalTransition({ title, prev, prevKey, next, nextKey, progress, className, ...rest }: {
    title?: string;
    prev: string;
    prevKey?: React.Key;
    next: string;
    nextKey?: React.Key;
    progress: number;
} & React.PropsWithoutRef<JSX.IntrinsicElements["div"]>): JSX.Element;
declare function InnerTerminalTransition({ prev, prevKey, next, nextKey, progress, }: {
    prev: string;
    prevKey?: React.Key;
    next: string;
    nextKey?: React.Key;
    progress: number;
}): JSX.Element;
export { MiniTerminalTransition, InnerTerminalTransition };
