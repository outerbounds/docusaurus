import React from "react";
declare function TerminalContent({ text, progress, style, }: {
    text: string;
    progress?: number;
    style?: React.CSSProperties;
}): JSX.Element;
export { TerminalContent };
