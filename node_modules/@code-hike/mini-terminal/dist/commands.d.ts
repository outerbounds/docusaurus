declare type CommandsInfo = {
    title: string;
    isRunning: boolean;
    commands: {
        run: string;
        output: string | null;
    }[];
};
declare function getCommands(text: string): CommandsInfo;
export { getCommands };
export type { CommandsInfo };
