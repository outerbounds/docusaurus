import React from "react";
export { MiniTerminal };
declare type MiniTerminalProps = {
    text?: string;
    title?: string;
    progress?: number;
    backward?: boolean;
    steps?: [{
        text: string;
    }];
} & React.PropsWithoutRef<JSX.IntrinsicElements["div"]>;
declare function MiniTerminal({ text, title, progress, backward, steps, ...rest }: MiniTerminalProps): JSX.Element;
