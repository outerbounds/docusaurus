import React from "react";
declare function MiniTerminalTransitions({ title, steps, progress, backward, ...rest }: {
    height?: number;
    title?: string;
    steps: {
        text: string;
    }[];
    progress: number;
    backward: boolean;
} & React.PropsWithoutRef<JSX.IntrinsicElements["div"]>): JSX.Element;
declare function InnerTerminalTransitions({ steps, progress, }: {
    steps: {
        text: string;
    }[];
    progress: number;
}): JSX.Element;
export { MiniTerminalTransitions, InnerTerminalTransitions };
