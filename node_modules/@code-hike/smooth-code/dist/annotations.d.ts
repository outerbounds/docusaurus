import { CodeAnnotation, InlineAnnotation, MultiLineAnnotation, TokenGroup, AnnotatedTokenGroups, FocusedLine, AnnotatedLine, LineGroup } from "./partial-step-parser";
import { Tween, FullTween, EditorTheme } from "@code-hike/utils";
export declare function parseAnnotations(annotations: Tween<CodeAnnotation[] | undefined> | undefined, theme: EditorTheme): {
    inlineAnnotations: FullTween<Record<number, InlineAnnotation[] | undefined>>;
    multilineAnnotations: FullTween<MultiLineAnnotation[]>;
};
export declare function annotateMultiline(lines: AnnotatedLine[], annotations: FullTween<MultiLineAnnotation[]>): FullTween<LineGroup[]>;
export declare function annotateInline(lines: FocusedLine[], inlineAnnotations: FullTween<Record<number, InlineAnnotation[] | undefined>>): {
    annotatedGroups: Tween<AnnotatedTokenGroups>[];
    lineNumber: Tween<number>;
    focused: FullTween<boolean>;
    move: "enter" | "exit" | "stay";
    enterIndex: number | null;
    exitIndex: number | null;
}[];
/**
 * Generate a list of annotated groups tweens
 * each annotated group contains a list of token groups and maybe an annotation.
 * The two annotated groups in a tween doesn't need to have the same token groups.
 */
export declare function annotateLineTokenGroups(tokenGroups: TokenGroup[], annotations: FullTween<InlineAnnotation[]>): Tween<AnnotatedTokenGroups>[];
