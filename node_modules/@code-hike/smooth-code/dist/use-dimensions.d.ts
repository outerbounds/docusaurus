import React from "react";
import { FocusString, Tween } from "@code-hike/utils";
declare type Dimensions = {
    containerWidth: number;
    containerHeight: number;
    deps: React.DependencyList;
    lineWidths: [number, number];
    lineWidth: [number, number];
    lineHeight: number;
    colWidth: number;
    lineNumberWidth: number;
} | null;
export { useDimensions, Dimensions };
declare function useDimensions(code: Tween<string>, focus: Tween<FocusString>, minColumns: number, lineNumbers: boolean, deps: React.DependencyList): {
    element: React.ReactNode;
    dimensions: Dimensions;
};
