import React from "react";
import { CodeShift } from "./partial-step-parser";
import { Dimensions } from "./use-dimensions";
export declare function SmoothContainer({ dimensions, codeStep, children, minZoom, maxZoom, center, progress, }: {
    dimensions: Dimensions;
    minZoom?: number;
    maxZoom?: number;
    center?: boolean;
    codeStep: CodeShift;
    children: (focusWidth: number, startX: number) => React.ReactNode;
    progress: number;
}): JSX.Element;
