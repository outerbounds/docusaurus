import { Code } from "@code-hike/utils";
import { CodeAnnotation } from "@code-hike/smooth-code";
import { Node, Parent } from "unist";
export declare function getAnnotationsFromMetastring(options: Record<string, string>): CodeAnnotation[];
export declare function extractAnnotationsFromCode(code: Code): readonly [CodeAnnotation[], string];
export declare function extractJSXAnnotations(node: Node, index: number, parent: Parent): CodeAnnotation[];
