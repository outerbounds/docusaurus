import { EditorStep } from "@code-hike/mini-editor";
import { Parent } from "unist";
export declare function extractStepsInfo(parent: Parent, config: {
    theme: any;
}, merge: "merge steps with header" | "merge step with previous"): Promise<EditorStep[]>;
