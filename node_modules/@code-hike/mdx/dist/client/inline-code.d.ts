import React from "react";
import { EditorTheme } from "@code-hike/utils";
export declare function InlineCode({ className, codeConfig, children, ...rest }: {
    className: string;
    children?: React.ReactNode;
    codeConfig: {
        theme: EditorTheme;
    };
}): JSX.Element;
