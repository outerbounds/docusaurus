import React from "react";
import { EditorProps, EditorStep } from "@code-hike/mini-editor";
import { PresetConfig } from "./preview";
export declare function Spotlight({ children, editorSteps, codeConfig, start, presetConfig, }: {
    children: React.ReactNode;
    editorSteps: EditorStep[];
    codeConfig: EditorProps["codeConfig"];
    start?: number;
    presetConfig?: PresetConfig;
}): JSX.Element;
