import { EditorTheme } from "@code-hike/utils";
export { TitleBar };
declare function TitleBar({ url, linkUrl, theme, }: {
    url: string;
    linkUrl: string;
    theme: EditorTheme;
}): JSX.Element;
